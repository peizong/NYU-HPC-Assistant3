#!/bin/bash
#set -e

# Activate Python virtual env used by OpenShift
#source ${APP_ROOT}/bin/activate

# Reinstall faiss-cpu with force to override swigfaiss if present
#pip install --no-cache-dir --force-reinstall faiss-cpu==1.7.4

# Now install the rest of the app dependencies
#pip install --no-cache-dir -r requirements.txt
